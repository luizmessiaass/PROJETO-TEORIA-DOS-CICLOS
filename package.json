{
  "name": "teoria-dos-ciclos",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page Teoria dos Ciclos - Relatórios Semanais.",
  "scripts": {
    "build": "node scripts/validate-static.mjs && node scripts/build-static.mjs",
    "extract": "playwright test scripts/extract-site.spec.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
